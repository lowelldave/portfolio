import React from "react";

import "./style.css";

const Loading = () => {

  return (
    <div className="loading">
        <label className="loading__label">
          Loading
        </label>
    </div>
  );
};

export default Loading;
